{
  "name": "authy-secrets",
  "version": "0.7.1",
  "description": "[DEPRECATED — use authy-cli instead] TypeScript SDK for the authy secrets manager",
  "deprecated": "This package has been replaced by authy-cli, which includes native Rust bindings. Install with: npm install authy-cli",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": {
        "types": "./dist/index.d.mts",
        "default": "./dist/index.mjs"
      },
      "require": {
        "types": "./dist/index.d.ts",
        "default": "./dist/index.js"
      }
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist",
    "prepublishOnly": "npm run build"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "keywords": [
    "secrets",
    "vault",
    "agents",
    "ai",
    "mcp",
    "authy"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/AuThY-org/authy",
    "directory": "packages/typescript"
  },
  "devDependencies": {
    "@types/node": "^25.3.0",
    "tsup": "^8.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
